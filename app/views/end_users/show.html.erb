<h1>end user mypage</h1>
<div class="container">
 <div class="row">
   <div class="col-4">
    <%= current_end_user.email %>
    <%= current_end_user.surname %> <%= current_end_user.name %>
          <td><%= link_to "編集",  edit_end_user_path(@end_user.id) %></td>
          <td><%= link_to "アカウント削除", end_user_path, data: { confirm: "退会させますか?" }, method: :delete %></td>
 </div>
</div>